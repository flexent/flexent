## Purpose

`@flexent/http-server` provides an idiomatic HTTP server API.

## Shared Repository Guidelines (Required)

All edits in this package must follow the shared repository guidelines in `../../GUIDELINES.md`.

## Do / Don't

- Keep exports explicit and stable.
- Keep the server integrates cleanly with the Dependency Injection container.
- Don't add external runtime dependencies unless absolutely necessary.
